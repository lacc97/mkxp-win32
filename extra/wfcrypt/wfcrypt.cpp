#include "wfcrypt.h"

#include "wintypes.h"

int wfcrypt_encrypt(char* plainText, int nb, int nk, const char* iv, const char* key, long plainTextLength) {
    return TRUE;
}

int wfcrypt_decrypt(char* cipherText, int nb, int nk, const char* iv, const char* key, long cipherTextLength) {
    return TRUE;
}
